# -*- perl -*-

# "pktdump" is a second interface to the libnetdissect library that is in progress

$testlist = [
    {
        program    => './pktdump',
        name       => 'pktdump-version',
        output     => 'pktdump-version.out',
    },
    {
        program    => './pktdump',
        name       => 'pktdump-read-print',
        output     => 'pktdump-read-print.out',
        args       => '--inputfile @TESTDIR@/ssh.pcap'
    },
    ];

1;
